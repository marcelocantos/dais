# TODO

- ~~Do Jevons minions survive jevonsd restart?~~ Done.
- ~~Can jevons-ctl be an internal tool (e.g. MCP server) instead of a separate binary?~~ Done — replaced with in-process MCP server.
- ~~Rename project from "dais" to "jevons" (Jevons paradox reference)~~ Done.
- Cache Lua scripts locally on the device so the connect screen renders from Lua even before connecting to jevonsd. Once caching works, remove the old purpose-built Swift views (ConnectView, ChatView, SessionListView, QRScannerView, SessionService, SessionSummary).
- **Session archaeology & transcript memory**: Ingest all existing Claude Code sessions from `~/.claude/projects/` into a searchable index. Maintain the index in realtime — watch the projects directory recursively via fsnotify for new/modified JSONL files and index new entries as they appear. Every Claude Code session (jevonsd-managed or standalone) feeds the index live. Consider sqlite FTS5 or [sqlite-lembed](https://github.com/asg017/sqlite-lembed) for semantic search. Jevons should be able to discover and resume any prior session, identify active vs stale work, and search across all project history. Bootstrap by ingesting everything; surface tools to filter/activate.
- OCR fallback for relay URL discovery: use `VNRecognizeTextRequest` (Vision framework) in the QR scanner to recognise `ws://` URLs from the camera feed when no QR code is present. Live Text on iOS 16+ could also work but integrated recognition is better UX.
- Audit SwiftUI modifier surface area and expose a moderate but fairly complete set as Lua-controllable props. Currently several behavioral modifiers are hardcoded in the Swift renderer (e.g. keyboard avoidance, scroll dismiss, autocorrection). These should be optional props on the relevant primitives so Lua scripts have full control. Areas to cover: keyboard behavior (ignore/avoid/dismiss), scroll behavior (anchoring, paging, indicators), text input (autocorrect, autocapitalization, keyboard type, secure entry), accessibility (labels, hints, traits), animation (transitions, implicit animations), gestures (long press, drag), safe area handling, and focus management.
